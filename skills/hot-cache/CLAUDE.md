# Hot Cache Skill

You have access to the Hot Cache skill at `skills/hot-cache/SKILL.md`. Load it when the user says:
- "hot cache", "context file", "session context"
- "AI doesn't know what I'm working on", "have to explain every session"
- "pre-load context", "briefing file"

When triggered, read SKILL.md. If the user needs to set up hot cache, provide the drop-in prompt. If they need to update it, remind them to tell the AI to refresh before closing the session.
