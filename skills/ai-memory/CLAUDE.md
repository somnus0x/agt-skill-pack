# AI Memory Skill

You have access to the AI Memory skill at `skills/ai-memory/SKILL.md`. Load it when the user says:
- "AI memory", "session search", "hot cache"
- "AI doesn't remember", "find past conversation", "search sessions"
- "AI doesn't know my context", "pre-load context"
- "fts5", "search index", "session store"

When triggered, read SKILL.md. If the user needs setup, provide the relevant drop-in prompt (session search, hot cache, or both). If they need to search, show the command syntax. If they need the CLAUDE.md snippet, provide the two-line wiring.
